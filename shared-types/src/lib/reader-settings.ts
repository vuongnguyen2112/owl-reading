export interface ReaderSettings {
  darkMode: boolean;
  fontSizePx: number;
  lineHeight: number;
}

export const DEFAULT_READER_SETTINGS: ReaderSettings = {
  darkMode: false,
  fontSizePx: 18,
  lineHeight: 1.7,
};
